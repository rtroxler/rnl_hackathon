<h2>New idea</h2>

<%= render "form.html", changeset: @changeset,
                        conn: @conn,
                        action: idea_path(@conn, :create) %>

<%= link "Back", to: idea_path(@conn, :index) %>
